{{range .Todos}}
    <li hx-delete="/api/todos/{{.Id}}" hx-trigger="click from:#delete-{{.Id}}">
        <label>
            {{if .Done}}
                <input type="checkbox" checked="checked" hx-post="/api/todos/{{.Id}}"/>
            {{else}}
                <input type="checkbox" hx-post="/api/todos/{{.Id}}"/>
            {{end}}
            <span>{{.Title}}</span>
        </label>
        <button id="delete-{{.Id}}">delete</button>
    </li>
{{end}}
